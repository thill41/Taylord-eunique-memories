<h1>Edit About</h1>

<%= form_with model: @about, url: abouts_path, method: :patch do |f| %>
  <div class="field">
    <%= f.label :content %>
    <%= f.rich_text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>